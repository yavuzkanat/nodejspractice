
mixin listProduct(product)
        .col
            .card.h-100.text-center
              img.card-img-top(src=`/img/${product.imgUrl}`, alt=product.name)
              .card-body
                h5.card-title #{product.name}
                p.card-text= product.description
              .card-footer.d-flex.justify-content-between.align-items-center.text-center
                span.text-muted ₺#{product.price}
                  a.btn.btn-outline-primary.btn-sm(href=`/details/${product.id}`) Details
                  br
                  a.btn.btn-outline-primary.btn-sm(href="#") Add To Cart